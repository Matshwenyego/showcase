import { FC } from "react";
import { Text } from "react-native";
import Base from "../../components/base";

const AboutScreen: FC = () => {
  return <Base component={<Text>About</Text>} />;
};

export default AboutScreen;
